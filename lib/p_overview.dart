import 'package:flutter/material.dart';

class POverview extends StatelessWidget {
  const POverview({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
        "Product Overview Page",
        style: TextStyle(fontSize: 25),
      ),
    );
  }
}
